# NorthStar 部署到 106.53.42.148

服务器环境已确认：Ubuntu 24.04.4 LTS、ubuntu 用户、约 4GB 内存、10GB swap、59GB 磁盘。

## 1. 本地上传（在 Mac 终端执行）

```bash
cd /Users/maggieewu/WorkBuddy/2026-08-28-23-40-33
scp northstar/northstar-deploy.tar.gz ubuntu@106.53.42.148:/tmp/
```

如果提示密码，输入腾讯云服务器的 ubuntu 用户密码；密码不会显示，这是正常的。

## 2. 服务器解压并部署（在服务器 WebShell 或 SSH 中执行）

```bash
sudo rm -rf /opt/northstar
sudo mkdir -p /opt/northstar
sudo tar -xzf /tmp/northstar-deploy.tar.gz -C /opt/northstar --strip-components=1
sudo chown -R ubuntu:ubuntu /opt/northstar
cd /opt/northstar/backend
bash deploy/deploy.sh
```

脚本已针对 Ubuntu 非 root 用户处理 `sudo`，会完成：

- Python 虚拟环境
- FastAPI 依赖
- `.env` 模板
- Nginx 静态托管
- `/api` 反代
- systemd 开机自启

本次压缩包已经包含前端 `dist/`，服务器没有 Node/npm 也可以部署。

## 3. 验证后端和页面

```bash
curl http://127.0.0.1:8000/api/health
curl http://106.53.42.148/api/health
```

浏览器打开：

```text
http://106.53.42.148
```

日志查看：

```bash
sudo systemctl status northstar --no-pager
sudo journalctl -u northstar -n 80 --no-pager
```

## 4. 安装视频号抓取所需浏览器

如果要启用视频号后台扫码和抓取，再执行：

```bash
sudo /opt/northstar/backend/.venv/bin/playwright install --with-deps chromium
```

如果暂时只看产品页面，可以先不安装这一项。

## 5. 登录注册验证

页面打开后点击右上角「登录」→「注册」，注册一个测试账号。

也可以先用接口验证：

```bash
curl -X POST http://106.53.42.148/api/auth/register \
  -H 'Content-Type: application/json' \
  -d '{"username":"maggie_test","password":"test123456","display_name":"Maggie"}'
```

返回 `token` 和 `user` 就说明注册链路正常。

## 6. 当前暂时不要做

- 不要删服务器上的 OpenClaw
- 不要安装 Docker
- 不要买 COS/CDN
- 不要先配置域名和 HTTPS
- 不要把 `.env` 或服务器密码发到聊天里

先用 IP 确认页面、API、登录都正常，再继续配置域名和 HTTPS。
