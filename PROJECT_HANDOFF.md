# NorthStar 项目交接

更新日期：2026-09-12  
代码仓库：<https://github.com/maggiewuwanjia-pixel/northstar>

## 一句话说明

NorthStar 是面向内容博主的经营与内容运营工作台：把已导入的直播、收入、短视频、商品与经营报表转为复盘、内容排期、知识库和行动建议。

产品原则：**真实数据与演示内容必须明确区分；没有官方接口时，不得把“导入”称为“自动同步”。**

## 当前真实能力

- 通过后台导出的 CSV / XLSX 导入直播、商品、经营、收入等报表。
- 导入收入记录并累计到经营指标。
- 导入直播数据后展示单场和月度复盘数据。
- 为已导入的真实视频记录显示内容卡片；没有真实视频时，显示明确标识的截图占位，而不是伪造“已同步”的视频。
- 首页和“全量内容排期”提供四周生产线甘特图：短视频、脚本、发布、直播选题脚本、开播五条轨道；全量页支持左右滑动切换周期。
- 全局搜索可搜索直播复盘、知识库、排期和脚本，并跳转到相应页面。
- 知识库和助手目前主要是结构化展示与规则化建议，**尚未接入真实大模型推理链路**。

## 明确不具备的能力

- 没有视频号官方开放接口 / 服务商授权时，无法自动同步历史直播、短视频、商品或经营数据。
- 不会自动抓取或保存直播回放、画面截图、语音证据。
- “同步助手”已改为数据导入助手，不能承诺扫码后同步全部历史数据。
- 竞品监控、多模态识别、自动选题/脚本、知识库 AI 整理目前只是产品方向或演示逻辑，不应对外宣称为可用 AI 服务。

## 数据与演示规范

1. 直播的观看、峰值在线、互动/热度、成交额等字段，以用户导入的报表为准。
2. 只有 `verified=1` 的视频才可作为“已导入真实短视频”出现在内容卡中。
3. 无真实视频或封面时应显示“待导入 / 演示占位”，不得使用生成图片假装来源于账号。
4. 11 月 13 日直播已手动补入一组真实趋势图与三个关键画面，其他场次没有回放素材时必须保持空态。
5. 任何 UI 文案不得声称“自动同步”“已采集回放”或“AI 已整理”，除非后台确有对应数据或任务结果。

## 当前项目结构

```text
frontend/                 Vue 前端
  src/views/              页面：Dashboard、Planning、LiveSummary、Wiki 等
  src/components/         导入、助手、渠道面板等
  src/stores/store.js     前端状态与 API 调用
  public/assets/live/     演示占位素材

backend/                  FastAPI 后端
  app/main.py             API 入口
  app/database.py         SQLite schema / migration
  app/services/scraper.py 报表导入与数据写入
  requirements.txt        含 openpyxl，用于 XLSX 导入
```

## 已实现的关键交互

- **数据导入**：渠道/数据导入面板支持 CSV、TSV、XLSX；XLSX 依赖 `openpyxl`。
- **直播复盘**：月度汇总、单场数据、趋势图、关键画面。无回放时展示空态和采集状态，不伪造。
- **内容排期**：首页北极星指标下方的四周甘特图和独立“全量内容排期”采用相同配色：绿=短视频，黑=直播，灰=脚本筹备，黄=热点。
- **手势翻页**：首页和全量排期支持触控/触控板横向滑动翻四周周期；仍保留按钮作为无触控设备备用。
- **内容表现 TOP**：无真实短视频时保留排版一致的截图占位卡片；有已验证导入数据时才替换。

## 部署现状

- 生产访问地址：`http://106.53.42.148`（域名尚未备案，因此当前使用 IP）。
- 服务器应用目录：`/opt/northstar`
- 后端：`/opt/northstar/backend`
- 前端构建产物：`/opt/northstar/frontend/dist`
- 服务：`northstar`（systemd）
- 数据库：`/opt/northstar/backend/data/northstar.db`
- Nginx 监听 80 端口并反向代理后端 / 静态托管前端。

不要把服务器私钥、数据库文件、环境变量、GitHub token 或平台登录 Cookie 提交到仓库。

## 常用部署流程

在本机项目目录中构建：

```bash
cd frontend
npm run build
```

将构建产物部署至服务器，并重启对应服务。部署前先备份；不要覆盖数据库、`.env` 或回放数据目录。后端变更后应安装依赖并重启 `northstar`，然后检查：

```bash
sudo systemctl status northstar
sudo nginx -t
```

## Git 与换机

仓库已经推送到 `main`。新电脑继续开发：

```bash
git clone https://github.com/maggiewuwanjia-pixel/northstar.git
cd northstar
```

机密信息不在仓库里，需要通过安全渠道单独配置：服务器 PEM、部署主机信息、环境变量、数据库备份及任何第三方平台凭证。

## 下一阶段优先级

1. **稳定导入链路**：为每类视频号报表建立可下载模板、字段映射、校验错误提示、导入历史与可撤销机制。
2. **真实素材链路**：支持用户上传真实视频文件、封面/截图，或填写公开链接；把素材绑定到对应直播/短视频记录。
3. **多账号与权限**：建立账号、团队、角色和数据隔离，不再把所有记录绑定到单一用户。
4. **AI 能力产品化**：接入真实模型服务后，基于已导入的文本、数据和媒体生成带来源引用的结论、追问和任务建议；展示模型状态、成本及失败空态。
5. **竞品能力合规化**：只处理用户有权获取的公开资料或明确授权的数据，提供来源、抓取时间和删除机制。
6. **生产工程化**：PostgreSQL/对象存储、异步任务队列、审计日志、备份恢复、监控告警、错误追踪、鉴权和限流。

## 开发注意事项

- 保留用户已有的改动；不要使用 `git reset --hard` 或覆盖数据库。
- 每次修改产品体验时，优先验证桌面、窄屏和触控板横向滑动。
- 修改导入或数据库 schema 后，需要在已有数据库上测试迁移。
- 对外展示前，逐项核实“真实 / 演示 / 未支持”状态，避免误导用户。
